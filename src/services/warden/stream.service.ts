// Warden SSE stream service — wraps Anthropic messages.stream into the
// Warden SSE event contract defined in contracts/warden-sse.md.
// Must run inside withTenant(companyId, fn). Implementation lands in T039/T040.
export {};
