// Warden conversations service — CRUD + turn persistence.
// All public functions MUST be called from inside withTenant(companyId, fn).
// Implementation lands in T038 (US1) and T063 (US2).
export {};
